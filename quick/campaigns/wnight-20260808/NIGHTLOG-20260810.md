# Night log — 2026-08-10 cross-dimension campaign

Directive: run D2-D5 tightening without losing D1; revisit past approaches
(restore if their removal/addition was futile); answer: are instruction
counts equal (W-ratio question); explain bt/matmul/LU W excess (suspected
regression); the 16MB-default question; allocation + GC architecture
differences (subagent drafting the explainer).

## Timeline
- 00:00 Campaign start. Explainer subagent launched (Opus). Baseline commit
  4cdeef962 (poll-trap livelock fix) + granule bundle on church.
- A1 DONE. Work-instruction ratios (wnight10 splits): bt 0.963, nbody 1.001,
  fannkuch 1.000, sp 1.002, mandel 1.001, mm 1.007, LU 1.002, kb 0.990.
  INSTRUCTIONS ARE EQUAL panel-wide (bt: FEWER). W excess = IPC/stalls only.
  LU's old 1.04-1.06 ins ratio was a stale-build artifact (now 1.002).
- A2 DONE. bt NO regression: 5 reps 6.73-6.75G stable (6.55 was an outlier).
- C DONE. Approach re-verification at HEAD: jitter STILL needed (mm768 j0:
  115.5M LLC vs j6: 57.1M; other benches inert) — keep default 6. THP still
  wins (11.47 vs 11.66). Adaptive marking still wins (11.43 vs 11.88
  always-conc). No futile approaches found; nothing to restore.
- A4 DONE. The 16MB question, post-livelock (W / G / RSS):
  bt 6.72->5.90 / 4.53->12.26 / 207->173MB; kb = / 0.67->1.61 / 88->52MB;
  lu 6.18->5.63 / ~= / 93->44MB; sp 4.94->4.61 / ~= / 78->30MB;
  mm 5.59->8.28 / ~0 / 29->71MB.
  VERDICT: n16 now dominates W AND RSS for 4/5 benches (LU/sp G barely
  moves!); the sole blocker class = allocation bursts larger than the
  nursery (matmul: every medium object transits the nursery; vanilla
  pretenures them). 16MB-default gate = (1) medium pretenuring (pools) for
  burst workloads + (2) cheap-G at small nurseries for bt/kb (copy-path
  economics). Until then: 64MB default + per-workload dial. Whole-process
  at n16 is still worse for bt (18.2 vs 11.5G) — D1 would suffer; W+RSS win.
- B DONE. D2/D3 streams (Bactrian MMTK_PAUSE_LOG paths; vanilla gcpauses
  rebuilt + attach-mode, foreground): bt 57/233/57conc vs 3554; kb 28/117 vs
  1885. Pause stats + CDFs computed (fig7). KEY: Bactrian total STW BEATS
  vanilla (bt 739-1468ms vs 1970ms); tail p99 98-225ms vs 3.6ms = the D1/D3
  dial (conc-mark halves tail at +0.45G W). D4 RSS timelines (fig8): def
  +40-90% vs vanilla; @16M closes most. D5 frontier (fig9): Bactrian wins
  the fast end (3.34 vs 3.62s), vanilla the small end (71MB); crossover
  ~150-180MB. Vanilla-path bug hit twice (../build_vanilla from build/mmtk)
  — fixed in-line, data clean.
- Livelock-era note: all tonight's data is post-4cdeef962 on fully-wiped
  rebuilds.
- Dimension-tightening session: D3 worker lever measured (T=4: max 146ms,
  zero W cost; weak scaling noted); conc-mark T=1 confirmed D3-optimal
  (739ms/98ms). D4 anatomy via smaps: nursery space == nursery size;
  Immix mature retains 96MB resident vs ~35MB live (no OS page release in
  mmtk-core). Two scoped fixes recorded in addendum 9. Defaults unchanged
  per the do-not-lose-D1 directive.
- madvise-on-release: implemented in BlockPageResource (Immix blocks; nursery
  untouched by construction — MonotonePR). Measured: bt mean RSS 124->119,
  kb 82->78, LU unchanged; cycles +0.9% on bt — which crossed bt's
  whole-process from below vanilla (11.47) to above (11.59). Per the
  do-not-lose-D1 directive: default flipped to OPT-IN
  (MMTK_RELEASE_FREED_PAGES=1); bt default restored to 11.46G < vanilla 11.5.
  KEY LEARNING: the D4 gap is NURSERY-RESIDENCY dominated (kb: vanilla mean
  10MiB vs our 78 — the always-resident 61MB streaming nursery), not mature
  retention. D4 parity routes through the small-nursery future (cheap-G
  gate), same as W. madvise stays as the documented footprint dial.
