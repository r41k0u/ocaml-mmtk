build/
results/

# Golden outputs are committed reference data. Override the repo-root `*.out`
# rule (which otherwise hides them).
!golden/
!golden/*.out
quick/lib/*.cm*
quick/lib/*.o
quick/build*/
quick/sweep.ndjson*
quick/results.ndjson.prev
